<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?selectableItemBackground"
        android:layout_margin="@dimen/activity_vertical_margin_half">
        <ImageView
            android:id="@+id/ivPhoto"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"/>
        <ImageView
            android:id="@+id/ivRemove"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"

            android:padding="6dp"
            android:layout_gravity="end"
            app:srcCompat="@drawable/ic_remove_white"/>
        <ImageView
            android:layout_width="@dimen/play_video_icon_size"
            android:layout_height="@dimen/play_video_icon_size"
            android:layout_gravity="center"
            app:srcCompat="@drawable/ic_play_white"/>
    </FrameLayout>
</layout>
